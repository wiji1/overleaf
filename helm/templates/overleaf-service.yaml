apiVersion: v1
kind: Service
metadata:
  name: overleaf
  namespace: overleaf
  labels:
    app: overleaf
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: overleaf
